{% extends 'base.html.twig' %}

{% block title %}Formulaire emprunteur
{% endblock %}

{% block body %}
	<h1>Formulaire Emprunteur</h1>

	{{ form_start(form) }}

	{{ form_row(form.nom) }}
	{{ form_row(form.prenom) }}
	{{ form_row(form.tel) }}
	<button type="submit" class="btn btn-primary">Valider</button>

	{{ form_end(form) }}
{% endblock %}
